# Model Manifest

Model weights are intentionally excluded from Git.

This document must be completed from the CURRENT WORKING MACHINE before a destructive reset.

For every required model asset record:

- exact filename
- exact source repository / URL
- exact local expected path
- exact file size
- SHA256
- format
- precision
- whether raw HF or converted production layout is expected
- whether the file is used by MLX, ComfyUI, or both

Do not guess missing values.

Do not substitute similarly named models.

Required model inventory has NOT yet been finalized.
