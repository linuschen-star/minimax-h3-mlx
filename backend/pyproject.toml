[project]
name = "minimax-h3-mlx"
version = "0.1.0"
description = "Native MLX backend for the full-quality MiniMax H3 inference graph"
requires-python = ">=3.10"
dependencies = ["mlx==0.32.0", "mlx-vlm==0.6.15", "numpy>=2", "scipy>=1.14", "tokenizers>=0.22"]

[tool.pytest.ini_options]
testpaths = ["tests"]
